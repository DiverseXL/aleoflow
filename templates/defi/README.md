# {{PROJECT_NAME}} - DeFi Template

A minimal Aleo program with deposit and withdraw functions for a liquidity pool.

## Structure

- `src/main.leo` — Contains `deposit` and `withdraw` transitions
- `program.json` — Program metadata

## Quick Start

```bash
leo build
leo test
```

## Extending

Add a `mapping` for balance tracking, implement record-based LP tokens, and add swap functionality.
