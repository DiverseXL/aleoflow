# {{PROJECT_NAME}} - AI Agent Template

A minimal Aleo program with an AgentState record and inference stub.

## Structure

- `src/main.leo` — Contains the `AgentState` record and `run_inference` function
- `program.json` — Program metadata

## Quick Start

```bash
leo build
leo test
```

## Extending

Replace the stub return values with real computation. Add more records for model parameters or training data.
