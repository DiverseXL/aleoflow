# {{PROJECT_NAME}} - GameFi Template

A minimal Aleo program with a PlayerState record and score submission.

## Structure

- `src/main.leo` — Contains the `PlayerState` record and `submit_score` function
- `program.json` — Program metadata

## Quick Start

```bash
leo build
leo test
```

## Extending

Add game mechanics like token rewards, achievement tracking, or leaderboard mappings.
