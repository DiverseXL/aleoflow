# {{PROJECT_NAME}} - Payment Template

A minimal Aleo program with a basic private transfer function.

## Structure

- `src/main.leo` — Contains the `transfer` transition
- `program.json` — Program metadata

## Quick Start

```bash
leo build
leo test
```

## Extending

Add balance tracking with a `mapping` and implement record-based transfers.
